{
  "name": "load-balancer-viz",
  "version": "1.0.0",
  "description": "A visual simulator for load balancing algorithms",
  "scripts": {
    "start:ssr": "http-server -p 8080",
    "generate:screenshots": "node scripts/generate_screenshots.js"
  },
  "devDependencies": {
    "http-server": "^14.1.1",
    "puppeteer": "^21.6.1"
  }
}
